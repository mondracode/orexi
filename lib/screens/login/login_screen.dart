import 'package:flutter/material.dart';

import 'package:orexi/screens/login/components/body.dart';

class Login extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}
